import React from "react";

function Listrender() {
  const slider = ["black", "red", "yellow"];
  const colorlist = slider.map((slider) => <li key={index}>{index}</li>);
  console.log(colorlist);
  return
   <>

   
  </>;
}

export default Listrender;
